# --- LLM ---
# openai 套件同時用於「雲端 OpenAI」與「本地端 OpenAI 相容伺服器（如 Ollama）」，
# 換本地 LLM 不需要額外套件，只要改 .env 的 LLM_PROVIDER / LLM_BASE_URL / LLM_MODEL。
openai>=1.40                 # 需支援 structured outputs（beta.chat.completions.parse）
python-dotenv>=1.0            # 讀取 .env

# --- 後端 API ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9      # FastAPI 檔案上傳需要

# --- RAG / 向量庫 ---
chromadb>=0.5
sentence-transformers>=2.7    # 多語系 embedding（支援繁體中文）
pypdf>=4.2                    # 解析 PDF 衛教單

# --- 前端 ---
streamlit>=1.35
requests>=2.31
