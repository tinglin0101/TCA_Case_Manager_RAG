[pytest]
testpaths = tests
addopts = -ra --strict-markers --tb=short
markers =
    unit: 純函式測試，不需要向量庫或模型（秒殺，隨時可跑）
    dataset: 題庫檔（data/eval/eval_questions.csv）本身的健檢
    retrieval: 需要已 ingest 的向量庫（python -m scripts.ingest）
    e2e: 需要真的呼叫 LLM，用 --e2e 開啟（慢）
    gate: MVP 驗收門檻；CI 可用 -m gate 只跑這些
